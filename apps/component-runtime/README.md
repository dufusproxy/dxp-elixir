# apps/component-runtime — client-side hydration runtime (Phase 2)

Placeholder. The client runtime that attaches interactivity to
server-rendered HTML (never re-renders it — spec principle #5) lands
here alongside the Vite plugin family (Track B).
