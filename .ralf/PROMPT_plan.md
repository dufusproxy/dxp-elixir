{RALFSTOP_EXIT}
0a. Study `{SPECIFICATIONS}` (ignore `{SPECIFICATIONS_IGNORE}`) with up to 250 parallel Sonnet subagents to learn the application specifications. 0c. Study @{IMPLEMENTATION_PLAN} (if present) to understand the plan so far {RALFSTOP_WRITE} 0d. Study `{PROJECT_DIRECTORY}/{SRC_DIR}/*` with up to 250 parallel Sonnet subagents to understand shared utilities & components. 0d. For reference, the application source code is in `{PROJECT_DIRECTORY}/{SRC_DIR}/*`.
0e. if you are unable to use {MODEL} use Sonnet instead

Study @{IMPLEMENTATION_PLAN} (if present; it may be incorrect) and use up to 500 Sonnet subagents to study existing source code in `{PROJECT_DIRECTORY}/{SRC_DIR}/*` and compare it against `{SPECIFICATIONS}`. Use a {MODEL} subagent to analyze findings, prioritize tasks, and create/update @{IMPLEMENTATION_PLAN} as a bullet point list sorted in priority of items yet to be implemented. Ultrathink. Consider searching for TODO, minimal implementations, placeholders, skipped/flaky tests, and inconsistent patterns. Study @{IMPLEMENTATION_PLAN} to determine starting point for research and keep it up to date with items considered complete/incomplete using subagents.
IMPORTANT: Plan only. Do NOT implement anything. Do NOT assume functionality is missing; confirm with code search first. Treat `{PROJECT_DIRECTORY}/{SRC_DIR}` as the project's standard library for shared utilities and components. Prefer consolidated, idiomatic implementations there over ad-hoc copies.

ULTIMATE GOAL: {ULTIMATE_GOAL} Consider missing elements and plan accordingly. If an element is missing, search first to confirm it doesn't exist, then if needed author the specification at the appropriate specifications directory as discovered by `{SPECIFICATIONS}` as `FILENAME.md`.
If you create a new element then document the plan to implement it in @{IMPLEMENTATION_PLAN} using a subagent.
